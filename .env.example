# cp .env.example .env  — gitignored; fill for local deploy
# CI uses GitHub Actions Variables with the same names (GCP_PROJECT_ID there).

HOST_PORT=
CONTAINER_NAME=
LOCAL_IMAGE=

GCP_PROJECT=
GCP_REGION=
SERVICE_NAME=
AR_REPO=
